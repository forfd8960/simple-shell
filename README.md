# Simple Shell

A simple shell written in Rust for learning purposes.

It supports basic command execution, piping, and redirection. 

## Features

- Command execution
- Piping
- Redirection
- Built-in commands (e.g., `cd`, `exit`)